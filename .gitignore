# ── Product image data only (large; kept on disk + RC, not in git) ──
# Everything else (datasets, embedding predictions, parquet, outputs,
# presentations, assets) IS tracked.
**/data/images/
**/amzn_shoes_monthly_avg_long_ffill_28_7_images/

# ── Docs (kept local only, not published) ──
docs/

# ── Regenerable cruft ──
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
